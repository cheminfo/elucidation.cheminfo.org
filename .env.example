# Copy to .env: cp .env.example .env
#
# Deployment mode: docker compose reads .env automatically for COMPOSE_FILE, so
# a bare `docker compose up -d` uses the file selected here.
COMPOSE_FILE=compose.traefik.yml
